import { render, fireEvent } from '@testing-library/react';
import DataTableView from './Podatki';

test('izračun in prikaz nadur', () => {
  //const { getByText } = render(<DataTableView />);
  //fireEvent.click(getByText('Nadure'));
  // Preverite, ali se prikaže pravilno sporočilo o nadurah
  //expect(getByText('Skupno število nadur:')).toBeInTheDocument();
});