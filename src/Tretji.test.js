import { render, fireEvent } from '@testing-library/react';
import GrafView from './Grafi';

test('prikaz grafa ob kliku na gumb', () => {
  //const { getByText } = render(<GrafView />);
  //fireEvent.click(getByText('Izračunaj ure'));
  // Preverite, ali se graf prikaže
  //expect(getByText('Delovne ure')).toBeInTheDocument();
});